// tslint:disable-next-line: const-enum
export enum Code {
    UNKOWN = -999,
    NETWORK_DOWN = -1,
    NO_RESPONSE = 0,
    PARAM_ERROR = 400,
    AUTH = 401,
    PERM_DENIED = 403,
    EMPTY = 404,
    SERVER_ERROR = 500,
    UNPARSEABLE_RESPONSE_HEADERS = 600,
}
